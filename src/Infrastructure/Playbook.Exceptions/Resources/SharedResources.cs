using Playbook.Exceptions.Core;

namespace Playbook.Exceptions.Resources;

/// <summary>
/// Marker class for shared or generic localization resources.
/// Acts as the default fallback resource type when no specific prefix 
/// is matched in the <see cref="LocalizedStringProvider"/>.
/// </summary>
public class SharedResources { }
